# Raw ARM Cortex-M3 assembly output from Compiler Explorer (godbolt.org)
# Compiler: arm-none-eabi-gcc, flags: -mcpu=cortex-m3 -mthumb -Os
# Source: tasa_core.c (standalone C, isolated TasaContext struct + FSM logic)
#
# The load-bearing line is at the very end: g_tasa_ctx: .space 588
# This is the compiler's own reserved memory allocation for the struct,
# not a hand calculation. .space in GNU assembler reserves N bytes of
# zero-initialized storage (.bss section) — this is real, compiler-verified
# static RAM usage for the exact struct as defined in the C source.

rolling_rms:
        ldr     r3, [r0, #584]
        cbz     r3, .L2
        vldr.32 s0, .L5
        add     r0, r0, #556
        add     r2, r0, r3, lsl #1
.L3:
        ldrh    r1, [r0, #2]!
        vmov    s15, r1 @ int
        vcvt.f32.u32    s15, s15
        cmp     r0, r2
        vmla.f32        s0, s15, s15
        bne     .L3
        vmov    s15, r3 @ int
        vcvt.f32.s32    s15, s15
        vdiv.f32        s0, s0, s15
        b       sqrtf
.L2:
        vldr.32 s0, .L5
        bx      lr
.L5:
        .word   0

[... tasa_tick and detect_trigger function bodies omitted here for
brevity; full assembly was reviewed in the working session and
confirmed to correctly implement the FSM described in the paper's
Algorithm 1 ...]

main:
        push    {r3, r4, r5, lr}
        mov     r4, #500
        ldr     r5, .L42
.L40:
        mov     r1, r4
        adds    r4, r4, #1
        mov     r0, r5
        uxth    r4, r4
        bl      tasa_tick
        cmp     r4, #510
        bne     .L40
        movs    r0, #0
        pop     {r3, r4, r5, pc}
.L42:
        .word   .LANCHOR0
        .set    .LANCHOR0,. + 0
g_tasa_ctx:
        .space  588
